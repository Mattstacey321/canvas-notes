# Use pnpm
engine-strict=true

# Always use exact versions
save-exact=true

# Strict dependency resolution
strict-peer-dependencies=true

# Faster installation
prefer-frozen-lockfile=true

# Link packages in monorepo
shamefully-hoist=false